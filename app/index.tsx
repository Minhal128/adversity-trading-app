/**
 * Entry point - redirects to main App component
 */
import React from 'react';
import { View } from 'react-native';

export default function Index() {
  // This file is not used with React Navigation
  // The main entry point is App.tsx
  return <View />;
}
